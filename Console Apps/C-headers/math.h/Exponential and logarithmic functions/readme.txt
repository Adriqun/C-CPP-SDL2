CO ROBI POSZCZEGOLNA FUNKCJA:

exp - Compute exponential function ( oblicz funkcje wykladnicza, Zwraca ona wartoœæ sta³ej e37 podniesionej do potêgi x )
frexp - Get significand and exponent ( uzyskaj mantysy i wykladnika )
ldexp - Generate value from significand and exponent ( generowanie wartosci z mantysy i wykladnika )
log - Compute natural logarithm ( oblicz logarytm naturalny )
log10 - Compute common logarithm ( oblicz logarytm,  logarytm dziesiêtny )

modf - Break into fractional and integral parts ( przerwa w czêœciach ulamkowych i integralnych )
exp2 - Compute binary exponential function ( oblicz binarna funkcje wykladnicza )
expm1 - Compute exponential minus one ( oblicza wykladnicza minus jeden )
ilogb - Integer binary logarithm ( calkowity logarytm binarny )
log1p - Compute logarithm plus one ( oblicza logarytm plus jeden )
log2 - Compute binary logarithm ( oblicz logarytm binarny )
logb - Compute floating-point base logarithm ( oblicz zmienno przecinkowa podstawe logarytmu )

scalbn - Scale significand using floating-point base exponent ( skalowanie znaczace, korzystajac ze zmiennoprzecinkowej bazy )
scalbln - Scale significand using floating-point base exponent (long) ( typu long )